interface AzureConfig { storageUrl: string; containerName: string; } interface AppConfig { apiBaseUrl: string; appName: string; appVersion: string; azure: AzureConfig; } export const config: AppConfig = { apiBaseUrl: import.meta.env.VITE_API_BASE_URL || '', appName: import.meta.env.VITE_APP_NAME || 'Cathedral Engage', appVersion: import.meta.env.VITE_APP_VERSION || '1.0.0', azure: { storageUrl: import.meta.env.VITE_AZURE_STORAGE_URL || '', containerName: import.meta.env.VITE_AZURE_CONTAINER_NAME || '', }, }; const requiredVars: string[] = ['VITE_API_BASE_URL', 'VITE_AZURE_STORAGE_URL', 'VITE_AZURE_CONTAINER_NAME']; requiredVars.forEach((varName: string) => { if (!import.meta.env[varName]) { console.error('Missing required environment variable: ' + varName); } });
